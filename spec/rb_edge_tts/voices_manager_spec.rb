# frozen_string_literal: true

require 'spec_helper'

RSpec.describe RbEdgeTTS::VoicesManager do
  describe '.create' do
    it 'creates a voices manager instance' do
      skip 'Requires network connection to fetch voices'
    end
  end

  describe '#find' do
    it 'finds voices by locale' do
      skip 'Requires network connection to fetch voices'
    end

    it 'finds voices by gender' do
      skip 'Requires network connection to fetch voices'
    end
  end
end
